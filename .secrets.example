# Local CI testing secrets (copy to .secrets and fill in values)
# NEVER commit .secrets to version control!
# These names must match the secrets referenced in per-OS workflows (uppercase)

NETBIRD_SETUP_KEY=
NETBIRD_MANAGEMENT_URL=
PROXMOX_HOST=
PROXMOX_USER=packer@pve
PROXMOX_PASSWORD=
PROXMOX_TOKEN=ci=
